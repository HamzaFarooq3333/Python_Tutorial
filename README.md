Enumerate function is a bulit in function that allows us to loop over a sequence (such as list ,tuple, or string) and get the index and value of each element in the sequence at the same time 
<br>
The indexing is handled by enumerate function by itself and we do not  have  to define an extra index variable for it.The index mostly starts from 0